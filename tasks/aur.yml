- name: Install zen browser AUR
  shell: yay --sudoloop --noconfirm -S zen-browser-bin
  tags:
    - install
    - dropbox

- name: Install spotify-edge AUR
  shell: yay --sudoloop --noconfirm -S spotify-edge
  tags:
    - install
    - dropbox
